# Core dependencies - pinned to known stable versions
# to avoid memory corruption from threading bugs in newer native libraries
google-api-python-client>=2.100.0,<2.200.0
youtube-transcript-api>=0.6.0,<2.0.0
requests>=2.28.0,<3.0.0
libsql>=0.1.0,<0.2.0
pyyaml>=6.0,<7.0
psycopg[binary]>=3.1.0,<3.4.0

# Pin protobuf to 5.x to avoid threading issues in protobuf 6.x
# See: https://github.com/protocolbuffers/protobuf/issues/
protobuf>=5.28.0,<6.0.0
